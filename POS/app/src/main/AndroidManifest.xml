<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.POS">
        <activity
            android:name=".Views.WelcomeActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.POS">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Views.PinActivity"
            android:exported="false"
            android:label="Verify PIN"
            android:theme="@style/Theme.POS" />

        <activity
            android:name=".Views.HomeActivity"
            android:exported="false"
            android:label="Home"
            android:theme="@style/Theme.POS" />

        <activity
            android:name=".Views.SettingsActivity"
            android:exported="false"
            android:label="Settings"
            android:theme="@style/Theme.POS" />

        <activity
            android:name=".Views.SaleActivity"
            android:exported="false"
            android:label="Sale"
            android:theme="@style/Theme.POS" />

        <activity
            android:name=".Views.ProfileActivity"
            android:exported="false"
            android:label="Profile"
            android:theme="@style/Theme.POS" />

        <activity
            android:name=".Views.NetworkActivity"
            android:exported="false"
            android:label="Network"
            android:theme="@style/Theme.POS" />

        <activity
            android:name=".Views.DisplayActivity"
            android:exported="false"
            android:label="Display"
            android:theme="@style/Theme.POS" />

        <activity
            android:name=".Views.SystemActivity"
            android:exported="false"
            android:label="System"
            android:theme="@style/Theme.POS" />

        <activity
            android:name=".Views.SecurityActivity"
            android:exported="false"
            android:label="Security"
            android:theme="@style/Theme.POS" />

        <activity
            android:name=".Views.SaleOptsActivity"
            android:exported="false"
            android:label="Sale Opts"
            android:theme="@style/Theme.POS" />

        <activity
            android:name=".Views.AboutActivity"
            android:exported="false"
            android:label="About"
            android:theme="@style/Theme.POS" />

        <activity
            android:name=".Views.ScanActivity"
            android:exported="false"
            android:label="Scan"
            android:theme="@style/Theme.POS" />

        <activity
            android:name=".Views.ManualActivity"
            android:exported="false"
            android:label="Manual"
            android:theme="@style/Theme.POS" />

        <activity
            android:name=".Views.RefundActivity"
            android:exported="false"
            android:label="Refund"
            android:theme="@style/Theme.POS" />

        <activity
            android:name=".Views.ArchivesActivity"
            android:exported="false"
            android:label="Archives"
            android:theme="@style/Theme.POS" />
    </application>

</manifest>