import React from 'react'
import ReactDOM from 'react-dom/client'
import PWCDashboard from '../PWC-system/PWC-Dashboard.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PWCDashboard />
  </React.StrictMode>,
)
