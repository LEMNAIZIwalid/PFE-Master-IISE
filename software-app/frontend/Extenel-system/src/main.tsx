import React from 'react'
import ReactDOM from 'react-dom/client'
import EXTDashboard from './EXT-Dashboard.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <EXTDashboard />
  </React.StrictMode>,
)
